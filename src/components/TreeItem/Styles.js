import styled from "styled-components"
import { colors } from "../../const"

export const StyledTreeItem = styled.div`
  text-align: left;
`
export const StyledTreeChild = styled.div`
  margin-top: 5px;
  padding: 0px 10px;
  border-radius: 4px;
  background-color: ${colors.lightOrange};
`
